services:
  openclaw:
    build: .
    container_name: openclaw
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/app
      - ./data:/app/data
    ports:
      - "3000:3000"
    environment:
      - TZ=Australia/Melbourne
