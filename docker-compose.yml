services:
  openclaw:
    build: .
    container_name: openclaw
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - TZ=Australia/Melbourne
